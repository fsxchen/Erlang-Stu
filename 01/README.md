## Erlang 的安装

输入erl:

启动erlang
erl
```
1> 20 + 30.
50
2> 
```
* 必须以.结尾

## 简单的运算

遵循基本的运算法则

## 变量
所有的变量必须以大写的字母开头。
**变量不变**


## 模式匹配
在大多数语言中`=`表示赋值，但是在Erlang中表示模式匹配

## 浮点数

## 原子
原子用来表示不同的非数字常量值。

## 元组
花括号和逗号构成。
### 创建元组
```
11> F={firstName, joe}.
{firstName,joe}
12> X,Y = F
12> .
{firstName,joe}
13> X.
4
14> Y.
{firstName,joe}
```
### 从元组中提取字段值
等号两边的元素必须含有相同数量的元素。
## 列表


